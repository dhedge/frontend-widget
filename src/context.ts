import { createContext } from "react";

export const PerformanceHistoryTabsContext = createContext(null as any);

export const InvestModalContext = createContext(null as any);

export const FundCompositionContext = createContext(null as any);

export const PoolAddressContext = createContext(null as any);

export const WalletInfoModalContext = createContext(null as any);

export const AvatarContext = createContext(null as any);
